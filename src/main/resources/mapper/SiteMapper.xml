<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.da.sage.assistant.mapper.SiteMapper">
  <cache flushInterval="3600000" size="10" />
  <select id="ALL_SITES">
    WITH
      T0 AS (
        SELECT DISTINCT
          TOP 100 FCY_0 AS Site
        FROM
          EXPLOIT.FACILITY
        WHERE
          MFGFLG_0 != 1
          OR SALFLG_0 != 1
          OR PURFLG_0 != 1
          OR WRHFLG_0 != 1
        ORDER BY
          FCY_0 DESC
      )
    SELECT
      'ALL' AS Site
    UNION ALL
    SELECT
      'CHINA' AS Site
    UNION ALL
    SELECT
      T0.Site
    FROM
      T0
  </select>
</mapper>